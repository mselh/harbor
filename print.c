
// Prints in as LED blinks, then will be UART when hardware cames by
// output is -1 if false
int print();

int print(int *seq) {
   // for now do nothing with seq, will go there later


    return 0;
}
